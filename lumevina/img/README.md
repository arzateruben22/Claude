# Lumevina logo & image assets

Drop brand image files here.

Expected files (upload what you have — one is fine):
- `logo-full.png` or `logo-full.svg` — the complete lockup
  (line-art woman-with-roses mark + "LUMEVINA / AESTHETICS SPA" wordmark).
  Used in the arrival intro overlay and the site footer.
- `logo-mark.png` or `logo-mark.svg` — just the circular mark, no text.
  Used in the top nav (small) and as the favicon / iOS app icon.

Format notes:
- SVG is best (vector — sharp at any size).
- Otherwise a **transparent** PNG, ~1200px+ wide. Avoid white-background JPGs.
